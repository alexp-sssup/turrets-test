{
  "name": "turrets-p0",
  "version": "0.1.0",
  "private": true,
  "description": "P0 prototype -- One Turret, One Lane. Headless simulation core.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test \"dist/test/**/*.test.js\"",
    "demo": "npm run build && node dist/src/app/main.js"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "~5.9.3"
  }
}
